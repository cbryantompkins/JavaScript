// the localeCompare string method compares strinss alphabetically
// returns a negative value if the reference string is lexicographically (alphabetically) before the compared string (the parameter),
// a positive value if it comes afterwards,
// and a value of 0 if they are equal

var a = "apple";
var b = "banana";

a.localeCompare(b);
// returns -1
